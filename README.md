## Instructions

Tester avec les commandes suivantes :

```sh
# Fonctionnel
cargo run -- example.txt 

# Retourne une erreur
cargo run --
cargo run -- empty.txt
cargo run -- hello

# Lance les tests
cargo test
```